

// namespace Wang.MathW
// {
//     internal static class NoiseUtility
//     {
//         static public int FastFloor(float value) => (int)(value >= 0 ? (int)value : (int)value - 1);

//         static public float Lerp(float a, float b, float t) => a + t * (b - a);

//         static public float Fade(float t) => t * t * t * (t * (t * 6 - 15) + 10);

//         static public float Dot(float firstX, float firstY, float secondX, float secondY) => (firstX * secondX + firstY * secondY);
//         static public float Dot(float firstX, float firstY, float firstZ, float secondX, float secondY, float secondZ) => 
//             (firstX * secondX + firstY * secondY + firstZ * secondZ);


//         public const float SQUARE_ROOT_2 = 1.4247691104677813f;

//     }
// }
